<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- stadium_info -->
<mapper namespace="com.henu.competition.mapper.StadiumInfoMapper">
    <!-- 字段映射 -->
    <resultMap id="stadiumInfoMap" type="com.henu.competition.model.StadiumInfo"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        si.id, si.name, si.city_id, si.address, si.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findStadiumInfoList" resultMap="stadiumInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM stadium_info si WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getStadiumInfo" resultMap="stadiumInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM stadium_info si WHERE 1 = 1
    </select>

</mapper>