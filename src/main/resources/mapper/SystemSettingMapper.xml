<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- system_setting -->
<mapper namespace="com.henu.competition.mapper.SystemSettingMapper">
    <!-- 字段映射 -->
    <resultMap id="systemSettingMap" type="com.henu.competition.model.SystemSetting"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ss.id, ss.name, ss.value
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findSystemSettingList" resultMap="systemSettingMap">
        SELECT
            <include refid="allColumns" />
        FROM system_setting ss WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getSystemSetting" resultMap="systemSettingMap">
        SELECT
            <include refid="allColumns" />
        FROM system_setting ss WHERE 1 = 1
    </select>

</mapper>