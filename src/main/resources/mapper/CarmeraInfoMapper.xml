<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- carmera_info -->
<mapper namespace="com.henu.competition.mapper.CarmeraInfoMapper">
    <!-- 字段映射 -->
    <resultMap id="carmeraInfoMap" type="com.henu.competition.model.CarmeraInfo"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ci.id, ci.nvr_id, ci.channel, ci.location, ci.status, ci.created_by, ci.creation_date, ci.last_updated_by,
        ci.last_update_date
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findCarmeraInfoList" resultMap="carmeraInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM carmera_info ci WHERE 1 = 1
        ORDER BY ci.CREATION_DATE DESC
    </select>

    <!-- 查询 -->
    <select id="getCarmeraInfo" resultMap="carmeraInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM carmera_info ci WHERE 1 = 1
    </select>

</mapper>