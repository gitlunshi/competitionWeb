<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- squadron_user_map -->
<mapper namespace="com.henu.competition.mapper.SquadronUserMapMapper">
    <!-- 字段映射 -->
    <resultMap id="squadronUserMapMap" type="com.henu.competition.model.SquadronUserMap"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        sum.id, sum.user_id, sum.squadron_id, sum.leader_tag, sum.creation_date, sum.last_updated_by, sum.last_update_date
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findSquadronUserMapList" resultMap="squadronUserMapMap">
        SELECT
            <include refid="allColumns" />
        FROM squadron_user_map sum WHERE 1 = 1
        ORDER BY sum.CREATION_DATE DESC
    </select>

    <!-- 查询 -->
    <select id="getSquadronUserMap" resultMap="squadronUserMapMap">
        SELECT
            <include refid="allColumns" />
        FROM squadron_user_map sum WHERE 1 = 1
    </select>

</mapper>