<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- sports_stadium_map -->
<mapper namespace="com.henu.competition.mapper.SportsStadiumMapMapper">
    <!-- 字段映射 -->
    <resultMap id="sportsStadiumMapMap" type="com.henu.competition.model.SportsStadiumMap"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ssm.id, ssm.sports_id, ssm.stadium_id, ssm.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findSportsStadiumMapList" resultMap="sportsStadiumMapMap">
        SELECT
            <include refid="allColumns" />
        FROM sports_stadium_map ssm WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getSportsStadiumMap" resultMap="sportsStadiumMapMap">
        SELECT
            <include refid="allColumns" />
        FROM sports_stadium_map ssm WHERE 1 = 1
    </select>

</mapper>