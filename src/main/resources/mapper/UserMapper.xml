<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user -->
<mapper namespace="com.henu.competition.mapper.UserMapper">
    <!-- 字段映射 -->
    <resultMap id="userMap" type="com.henu.competition.model.User"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        u.id, u.name, u.password, u.signature, u.sex, u.birthday, u.photo, u.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findUserList" resultMap="userMap">
        SELECT
            <include refid="allColumns" />
        FROM user u WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getUser" resultMap="userMap">
        SELECT
            <include refid="allColumns" />
        FROM user u WHERE 1 = 1
    </select>

</mapper>