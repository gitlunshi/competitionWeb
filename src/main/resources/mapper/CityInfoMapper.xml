<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- city_info -->
<mapper namespace="com.henu.competition.mapper.CityInfoMapper">
    <!-- 字段映射 -->
    <resultMap id="cityInfoMap" type="com.henu.competition.model.CityInfo"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ci.id, ci.name, ci.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findCityInfoList" resultMap="cityInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM city_info ci WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getCityInfo" resultMap="cityInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM city_info ci WHERE 1 = 1
    </select>

</mapper>