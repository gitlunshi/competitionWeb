<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- resource_info -->
<mapper namespace="com.henu.competition.mapper.ResourceInfoMapper">
    <!-- 字段映射 -->
    <resultMap id="resourceInfoMap" type="com.henu.competition.model.ResourceInfo"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ri.id, ri.user_id, ri.city_id, ri.sport_id, ri.stadium_id, ri.title, ri.wenan, ri.cover_url,
        ri.resource_url, ri.resource_duration, ri.share_count, ri.praise_count, ri.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findResourceInfoList" resultMap="resourceInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM resource_info ri WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getResourceInfo" resultMap="resourceInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM resource_info ri WHERE 1 = 1
    </select>

</mapper>