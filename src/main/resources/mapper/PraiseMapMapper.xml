<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- praise_map -->
<mapper namespace="com.henu.competition.mapper.PraiseMapMapper">
    <!-- 字段映射 -->
    <resultMap id="praiseMapMap" type="com.henu.competition.model.PraiseMap"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        pm.id, pm.user_id, pm.resource_id, pm.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findPraiseMapList" resultMap="praiseMapMap">
        SELECT
            <include refid="allColumns" />
        FROM praise_map pm WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getPraiseMap" resultMap="praiseMapMap">
        SELECT
            <include refid="allColumns" />
        FROM praise_map pm WHERE 1 = 1
    </select>

</mapper>