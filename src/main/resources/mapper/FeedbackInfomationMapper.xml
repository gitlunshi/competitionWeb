<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- feedback_infomation -->
<mapper namespace="com.henu.competition.mapper.FeedbackInfomationMapper">
    <!-- 字段映射 -->
    <resultMap id="feedbackInfomationMap" type="com.henu.competition.model.FeedbackInfomation"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        fi.id, fi.user_id, fi.app_version, fi.phone_model, fi.system_version, fi.feedback_content
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findFeedbackInfomationList" resultMap="feedbackInfomationMap">
        SELECT
            <include refid="allColumns" />
        FROM feedback_infomation fi WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getFeedbackInfomation" resultMap="feedbackInfomationMap">
        SELECT
            <include refid="allColumns" />
        FROM feedback_infomation fi WHERE 1 = 1
    </select>

</mapper>