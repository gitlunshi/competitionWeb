<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- share_map -->
<mapper namespace="com.henu.competition.mapper.ShareMapMapper">
    <!-- 字段映射 -->
    <resultMap id="shareMapMap" type="com.henu.competition.model.ShareMap"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        sm.id, sm.user_id, sm.resource_id, sm.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findShareMapList" resultMap="shareMapMap">
        SELECT
            <include refid="allColumns" />
        FROM share_map sm WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getShareMap" resultMap="shareMapMap">
        SELECT
            <include refid="allColumns" />
        FROM share_map sm WHERE 1 = 1
    </select>

</mapper>