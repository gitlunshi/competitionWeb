<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- sports_type -->
<mapper namespace="com.henu.competition.mapper.SportsTypeMapper">
    <!-- 字段映射 -->
    <resultMap id="sportsTypeMap" type="com.henu.competition.model.SportsType"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        st.id, st.icon, st.name, st.status
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findSportsTypeList" resultMap="sportsTypeMap">
        SELECT
            <include refid="allColumns" />
        FROM sports_type st WHERE 1 = 1
    </select>

    <!-- 查询 -->
    <select id="getSportsType" resultMap="sportsTypeMap">
        SELECT
            <include refid="allColumns" />
        FROM sports_type st WHERE 1 = 1
    </select>

</mapper>