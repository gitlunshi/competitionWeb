<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- project_info -->
<mapper namespace="com.henu.competition.mapper.ProjectInfoMapper">
    <!-- 字段映射 -->
    <resultMap id="projectInfoMap" type="com.henu.competition.model.ProjectInfo"/>

    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        pi.id, pi.name, pi.group, pi.created_by, pi.creation_date, pi.last_updated_by, pi.last_update_date
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- 分页查询列表 -->
    <select id="findProjectInfoList" resultMap="projectInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM project_info pi WHERE 1 = 1
        ORDER BY pi.CREATION_DATE DESC
    </select>

    <!-- 查询 -->
    <select id="getProjectInfo" resultMap="projectInfoMap">
        SELECT
            <include refid="allColumns" />
        FROM project_info pi WHERE 1 = 1
    </select>

</mapper>